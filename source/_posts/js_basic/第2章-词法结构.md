---
title: 犀牛-第2章-词法结构
permalink: 词法结构
date: "2019/7/12"
mathjax: true
tags: 
- JS
- 犀牛书
categories: JS
---

# 第二章 词法结构

## 2.1字符集

JavaScript程序的内容使用Unicode编码编写保存。

## 2.1.1区分大小写

HTML是不区分大小写的语言，但是JavaScript是区分大小写的，也就是说，关键字、变量、函数名和所有的标识符都必须采用一致的大小写形式。

## 2.1.2空格、换行符和格式控制符

JS会忽略程序中标识之间的空格。具体有哪些Unicode码看书。

## 2.1.3Unicode转义字符

转义字符使用6个ASCII码表示任意的16位的Unicode内码。使用`\u`开头，其后跟随4个16进制的数。这种Unicode转义字符可以在JS的字符串直接量、正则表达式直接量和标识符中使用(关键字除外)。当然也可以出现在注释中，但是会被忽略，不会再显示时解析为Unicode字符，保持6个ASCII码进行显示。

## 2.1.4标准化

Unicode允许使用多种方式对同一个内码(字符)进行编码。例子看书。也就是说同一个内码保存在计算机内存上的二进制编码是不同的，但是解析后在屏幕上显示结果一样。Unicode为所有的字符都定义了一个首选的编码格式，并给出了一个标准化的处理方式来将文本转化为一种比较标准的格式，但是JS认为其解析的所有程序代码都是这种标准格式，不会再对其标识符、字符串或正则表达式做标准化处理。

## 2.2注释

两种注释：多行注释/**/和单行注释//

## 2.3直接量

所谓直接量，就是直接在程序中能够直接使用的数据值。不同数据类型的都有自己直接量的写法。后面会详细讲解。比如：
```
12
1.2
"hello world"
'Hi'
true false
/javascript/gi
null NaN undefined
```

## 2.4标识符和保留字

标识符就是一个名字，用来对变量和函数进行命名，或者作为循环中跳转位子的标记。

标识符由字母、数字、下划线(_)和美元符号($)组成，同时数字不当头。虽然JS允许使用Unicode字符全集中的字母和数字，但通常我们只是用ASCII码和数字来书写标识符。

保留字不能作为普通的标识符。

**保留字**

- JS把一些标识符拿出来作为自己的关键字。具体哪些看书。

- 严格模式下保留字列表稍有不同。

- JS预定义和很多全局变量和函数。避免标识符与其重复。

- JS具体实现可能会有更多的全局变量和函数。每一个运行环境都有自己的一套变量和函数。DOM+BOM

## 2.5可选的分号

使用分号分割语句。

当不使用分号时的会自动解析并在换行处添加分号。规则如下

1. 一般来说，如果换行处缺失了分号，且在换行处无法与下一行正确合并解析的时候，才会添加分号一般来说( `(` `[` `+` `-` `/`等符号)容易与上一行合并解析。

2. `return break continue`后换行会直接添加分号

3. `++` `--`运算符可以作为前缀和后缀，在换行合并解析时，如果要解析为后缀，那么必须与表达式在同一行，否则将添加分号作为下一行代码的前缀(说实话这些内容对开发来说都不重要)
